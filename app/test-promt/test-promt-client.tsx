"use client";

import { useState } from "react";
import styles from "./test-promt.module.css";

const prompt = "Actua como un ingeniero senior especializado en tests de integracion de APIs.\n\nTienes acceso completo al repositorio desde VS Code. Debes ampliar la suite de integracion existente, respetando su arquitectura, herramientas y convenciones.\n\nEl repositorio es la unica fuente de verdad. No asumas frameworks, contratos, codigos HTTP, rutas ni infraestructura que no hayas confirmado en el codigo o en los requisitos.\n\n# Objetivos\n\nEn cada ejecucion debes:\n1. Identificar los cambios desde la ultima revision.\n2. Detectar endpoints añadidos, modificados o eliminados.\n3. Relacionar cambios con la cobertura existente.\n4. Analizar endpoints solicitados o afectados.\n5. Diseñar todos los tests de integracion aplicables.\n6. Compararlos con los tests existentes.\n7. Presentar una matriz de cobertura para aprobacion.\n8. Implementar solamente los tests aprobados.\n9. Ejecutar los tests y corregirlos hasta dejarlos verdes.\n10. Mantener trazabilidad entre commits, endpoints, tests y resultados.\n\nNo crees infraestructura de testing nueva si el repositorio ya tiene una funcionando.\n\n# Fase 1: estudiar el proyecto\n\nAntes de modificar archivos:\n- Lee las instrucciones del repositorio.\n- Localiza los proyectos de tests de integracion.\n- Identifica framework, fixtures, factories, colecciones, cliente HTTP, base de datos, limpieza, autenticacion, autorizacion, fakes, mocks, convenciones y comandos oficiales.\n- Busca tests del mismo endpoint y de endpoints parecidos.\n- Reutiliza helpers e infraestructura existente.\n\nNo construyas una factory, autenticacion, base de datos o limpieza paralela salvo que demuestres que la actual no puede cubrir el caso.\n\n# Fase 2: seguimiento mediante Git\n\nBusca TEST_TRACEABILITY.md. Si no existe, crea un unico documento dentro de la estructura de tests. Registra ultimo commit analizado y probado, rama, fecha, rango Git, cambios locales, endpoints afectados, tests, comandos, resultados, defectos y cobertura pendiente.\n\nAl comenzar ejecuta:\n\ngit branch --show-current\ngit rev-parse HEAD\ngit log --oneline <ultimo-commit>..HEAD\ngit diff --name-status <ultimo-commit>..HEAD\ngit diff <ultimo-commit>..HEAD\ngit status --short\ngit diff\ngit diff --cached\n\nSi es la primera ejecucion, registra el estado actual como linea base. No hagas commit, push, merge ni rebase salvo peticion expresa.\n\n# Fase 3: detectar cambios funcionales\n\nBusca endpoints nuevos, eliminados o modificados, rutas, metodos HTTP, DTOs, codigos de estado, validaciones, permisos, reglas, persistencia, transacciones, dependencias, eventos y regresiones. Presenta:\n\n| Cambio | Commit | Endpoint afectado | Impacto funcional | Cobertura actual | Accion necesaria |\n| ------ | ------ | ----------------- | ----------------- | ---------------- | ---------------- |\n\nAcciones: Ninguna, Añadir test, Ampliar test, Actualizar test, Añadir regresion o Requiere confirmacion.\n\n# Fase 4: analizar cada endpoint\n\nSigue el flujo completo: metodo y ruta, parametros, DTO, binding, validadores, autenticacion, autorizacion, controller o handler, servicios, repositorios, entidades, invariantes, base de datos, transacciones, excepciones, middleware, response DTO, eventos, efectos secundarios, dependencias externas y tests existentes.\n\nDistingue entre confirmado por implementacion, confirmado por requisito, recomendado y requiere confirmacion. No inventes requisitos.\n\n# Estandar obligatorio de cobertura\n\nEvalua todas las categorias aplicables e indica cuando no aplican.\n\n1. Happy path: status exacto, Content-Type, estructura completa, valores relevantes, persistencia, relaciones, efectos secundarios y ausencia de cambios inesperados.\n\n2. Request invalido: body ausente, JSON mal formado, campo ausente, null, cadena vacia o con espacios, formato invalido, tipo incorrecto, rango, limites, enum y mass assignment cuando aplique.\n\n3. Identificadores y recursos: ID invalido, vacio, recurso inexistente, relaciones inexistentes, claves foraneas, recurso inactivo y acceso ajeno.\n\n4. Autenticacion y autorizacion: credenciales ausentes o invalidas, token expirado, usuario sin permiso, rol incorrecto, recursos ajenos y datos sensibles. Distingue autenticacion de autorizacion.\n\n5. Reglas de negocio: duplicados, conflictos, invariantes, estados invalidos, transiciones, operaciones repetidas y limites.\n\n6. Persistencia y atomicidad: datos persistidos, campos generados, relaciones, fechas, estados, registros modificados, rollback y ausencia de cambios parciales.\n\n7. Dependencias externas: respuesta correcta, error funcional, timeout, servicio no disponible, respuesta invalida, excepcion, error contractual, retries y circuit breaker observables.\n\n8. Idempotencia y concurrencia: repeticion, clave repetida, operaciones simultaneas, duplicados por carrera y concurrencia optimista cuando exista riesgo real.\n\nPor tipo: GET individual existente, inexistente e ID invalido; GET coleccion con resultados, vacia, paginacion, filtros y ordenamiento; POST exitoso, invalido, duplicado y relaciones; PUT/PATCH actualizado, inexistente, invalido, campos protegidos y conflicto; DELETE exitoso, inexistente, repetido, dependencias y eliminacion fisica o logica.\n\n# Valoracion de respuestas\n\nComprueba status, headers, Content-Type, body, contrato, tipos, valores, errores estructurados, ausencia de secretos y estado final. Clasifica como Correcto, Correcto con observaciones, Defecto productivo, Contrato ambiguo o Bloqueado.\n\nPara cada error determina si el codigo HTTP representa bien el fallo, si el cliente puede corregirlo, si es consistente con endpoints similares, si expone informacion sensible y si produjo efectos parciales.\n\n# Fase 5: comparar cobertura\n\nClasifica cada escenario como Cubierto, Parcialmente cubierto, No cubierto, Duplicado, Cubierto en otro archivo, Requiere confirmacion o Bloqueado por infraestructura. Si un test es insuficiente, explica que assertion falta. No dupliques tests equivalentes.\n\n# Entrega inicial: detenerse antes de implementar\n\nPresenta cambios desde la ultima ejecucion, endpoints detectados, resumen de cada endpoint, cobertura existente con rutas, matriz con ID, prioridad, categoria, escenario, estado, preparacion, resultado y evidencia, tests faltantes, tests a ampliar, valoracion, riesgos, defectos y preguntas pendientes.\n\nDespues detente y espera aprobacion, salvo que el usuario haya autorizado analisis e implementacion completa.\n\n# Fase 6: implementacion aprobada\n\nImplementa solo escenarios aprobados. Reutiliza fixtures, factories, helpers, DTOs y contratos reales. Mantén tests independientes, repetibles y deterministas. No uses mocks para sustituir lo que el test pretende integrar. No debilites ni deshabilites tests existentes. No cambies codigo productivo para hacer pasar un test. Documenta defectos por separado.\n\n# Fase 7: ejecucion\n\nDescubre comandos oficiales y ejecuta: 1) tests nuevos o modificados, 2) clase, modulo o feature afectada, 3) suite completa de integracion. Registra comando, cantidad, aprobados, fallidos, omitidos, duracion y causa. Corrige tus cambios hasta dejar la suite verde. Distingue fallos nuevos, defectos productivos, fallos preexistentes, entorno, tests obsoletos y ambigüedad.\n\n# Fase 8: trazabilidad final\n\nActualiza el ultimo commit analizado al finalizar el analisis. Registra como ultimo commit probado solo uno cuyos tests se ejecutaron. Separa cambios locales de HEAD. Incluye fecha, rama, rango Git, endpoint, cambio, tests, resultado y pendientes.\n\n# Entrega final\n\nPresenta rama y commit, rango revisado, endpoints, archivos modificados, tests añadidos o ampliados, escenarios, valoraciones, comandos y resultados exactos, fallos preexistentes, defectos, riesgos y cobertura pendiente.\n\n# Alcance solicitado\n\nEndpoint o funcionalidad:\n[INDICA AQUI EL ENDPOINT, CONTROLADOR, MODULO O FUNCIONALIDAD]\n\nRequisitos adicionales:\n[OPCIONAL: INDICA AQUI REQUISITOS, BUGS O COMPORTAMIENTOS ESPERADOS]\n\nModo de ejecucion:\n[INDICA ANALISIS Y ESPERAR APROBACION O ANALISIS E IMPLEMENTACION COMPLETA]";

export default function TestPromtClient() {
  const [copyLabel, setCopyLabel] = useState("Copiar prompt");

  async function copyPrompt() {
    await navigator.clipboard.writeText(prompt);
    setCopyLabel("Copiado");
    window.setTimeout(() => setCopyLabel("Copiar prompt"), 1400);
  }

  return (
    <main className={styles.page}>
      <section className={styles.shell}>
        <header className={styles.header}>
          <div><p className={styles.eyebrow}>Prompt de trabajo</p><h1>Test Integration Review</h1><p className={styles.subtitle}>Prompt para analizar, diseñar y verificar tests de integracion de APIs.</p></div>
          <button type="button" onClick={copyPrompt} className={styles.copyButton}>{copyLabel}</button>
        </header>
        <div className={styles.promptBox}><pre>{prompt}</pre></div>
      </section>
    </main>
  );
}
