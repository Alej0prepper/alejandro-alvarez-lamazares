import { describe, expect, it } from "vitest";
import { parseLogisticsRows, parseLooseNumber } from "./parse-logistics-workbook";
const rows = [["", "Control logística comercial interna NWI 10.07.2026"], ["Item", "Depósito In bond NWI- ZELCOMSA.", "", "En consignación Hums -CIMEX."], ["", "Autos", "PPA-Insumos", "Habana", "Villa Clara", "Santiago Cuba"], ["Ej-1", "127 carros en deposito - 36 facturados.", "Inv 1 033 809.91 eur VA 524 363.91 eur", "Inv- 149.427.54 US.- VA- .30 614.30 US. 56 AUTOS EN DEPOSITO"], ["EJ-2", "En consignación tiendas PALCO."], ["", "Sabory.", "Industrial Guanabo.", "Peñas Alta"], [], ["Ej-3", "Cargas variadas."], ["", "Agrupadas en Carilog-Comerciales.", "Agrupadas comerciales-Aero Varadero", "Operativas-clientes", "", "Menajes"], ["", "", "", "Producción", "Embarcadas", "Proceso embarque", "Embarcadas"], ["", "5 pallets pintura marcacion de carretera"]] as const;
describe("logistics parser", () => { it.each([["149.427.54", 149427.54], [".30 614.30", 30614.3], ["1 033 809.91", 1033809.91], ["524 363.91 eur", 524363.91]])("parses %s", (input, expected) => expect(parseLooseNumber(input)).toBe(expected)); it("keeps empty values as null", () => { const result = parseLogisticsRows([["Control logística comercial interna NWI"]], { fileName: "empty.xlsx", sheetName: "Sheet1" }); expect(result.ok).toBe(true); if (result.ok) expect(result.data.inBondVehicles.deposited).toBeNull(); }); it("extracts the report", () => { const result = parseLogisticsRows(rows, { fileName: "control.xlsx", sheetName: "Sheet1" }); expect(result.ok).toBe(true); if (!result.ok) return; expect(result.data.reportDate).toBe("10.07.2026"); expect(result.data.inBondVehicles.deposited).toBe(127); expect(result.data.inBondVehicles.invoiced).toBe(36); expect(result.data.inBondVehicles.pendingInvoice).toBe(91); expect(result.data.consignmentVehicles.deposited).toBe(56); expect(result.data.nwiValues.inventory).toBe(1033809.91); expect(result.data.nwiValues.va).toBe(524363.91); expect(result.data.humsValues.inventory).toBe(149427.54); expect(result.data.humsValues.va).toBe(30614.3); expect(result.data.quantifiedCargo[0]?.quantity).toBe(5); }); });
